import { Show } from "../types/dtos"

class ShowService {
  // Will search the DB if the show already exists
  // getShowByIMDBId = async (imdbId: string): Promise<Show> => {
  //   return {}
  // }
}

export default new ShowService()
